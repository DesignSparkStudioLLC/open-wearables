# Backend API base URL.
#
# PUBLIC_ prefix is required by SvelteKit for anything reachable from the
# browser. Read through $env/dynamic/public so it stays a *runtime* value: a
# prebuilt image can be pointed at any backend without rebuilding.
PUBLIC_API_URL=http://localhost:8000
